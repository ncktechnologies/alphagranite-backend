"""
Tests package initializer.

Making the `tests` directory a package so tests can import using
`from tests...` during pytest collection.
"""

# Intentionally left minimal. If additional test-time path tweaks are
# required later, we'll add them to `tests/conftest.py`.
